{
  "name": "alvai-api",
  "version": "1.0.0",
  "description": "BLEU.LIVE — Alvai AI Chat API + Search + Affiliate Tracking",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
