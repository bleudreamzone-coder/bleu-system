# Day 80 deploy verification — 2026-05-26

Merged `day79-scaffold` → `main` (`ed23b9a..e37a6e8`, merge commit `e37a6e8`,
8 commits) and pushed. Render auto-deployed the web service `bleu-system`.

## Production endpoint checks (https://bleu-system.onrender.com)

| Endpoint | Expected | Got |
|---|---|---|
| `GET /api/ping` | 200 | ✅ 200 |
| `POST /api/auth/verify` {token:bad} | 401 | ✅ 401 (route exists → new code live) |
| `POST /api/auth/magic-link` {email:no-`@`} | 200, no row | ✅ 200 |
| `POST /api/sms/inbound` (unsigned) | 403 | ✅ 403 |

No prod rows created by these checks (garbage email skips row creation; bad token
matches nothing; unsigned inbound rejected before any write).

## State after deploy
- Migrations `bleu_comms` + `magic_links`: live, RLS on, grants none.
- Auth/SMS/email endpoints: live in prod.
- Front-end magic-link handler: still gated `BLEU_AUTH_ENABLED=false` (inert until flip).
- `RESEND_API_KEY`: set in Render → first real magic-link email will send (not deferred) on next live request. Local smoke ran with it unset by design.

## Outstanding
- **Rotate the Supabase service_role key** — it was exposed in chat during smoke. Reset in Supabase → API, update Render `SUPABASE_SERVICE_KEY`.
- Day-81 queue: flip `BLEU_AUTH_ENABLED`, resolve `citizen_id` in Stripe webhook, phone capture on `bleu_citizens`, register day-7 cron in render.yaml, consolidate the two Twilio webhooks, retrofit `logTrustPacket` onto routes.
